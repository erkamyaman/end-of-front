/**
 * Events & event delegation: bubbling/capturing.
 *
 * Angular tie-in: what (click)="handler()" compiles down to, and why delegation
 * matters for performance in a long *ngFor list of clickable rows.
 */
