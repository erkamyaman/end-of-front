/**
 * Browser storage: localStorage, sessionStorage, cookies.
 *
 * Angular tie-in: common pattern of persisting NgRx/Signal state to localStorage,
 * and the JSON serialization gotchas from Stage 1 come back here.
 */
