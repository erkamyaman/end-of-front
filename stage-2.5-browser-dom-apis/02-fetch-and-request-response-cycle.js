/**
 * fetch and the native request/response cycle — what HttpClient is wrapping.
 *
 * Angular tie-in: directly compare a raw fetch() call to the equivalent
 * HttpClient.get() to see exactly what Angular adds (interceptors, typed
 * responses, RxJS integration).
 */
