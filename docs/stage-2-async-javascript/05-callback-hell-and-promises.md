# Callback hell & why Promises replaced it

<Badge type="info" text="todo" />
`stage-2-async-javascript/05-callback-hell-and-promises.js`

nested pyramid-of-doom vs flat .then() chains.

Angular tie-in: seeing why RxJS operators (switchMap etc.) exist for the exact same "flatten nested async" reason.
