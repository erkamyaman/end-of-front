# 06 Type coercion vs casting

<Badge type="info" text="todo" />
`stage-1-core-javascript/06-type-coercion-vs-casting.js`

implicit vs explicit, equality algorithms (== vs === vs Object.is).

Angular tie-in: truthy/falsy coercion inside *ngIf, why *ngIf="0" and *ngIf="''" both hide the element.
