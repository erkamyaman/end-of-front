# Stage 4.5 — Accessibility

## Semantic HTML — the right element instead of div-with-a-click-handler.

<Badge type="info" text="todo" />

`01-semantic-html.js`

Angular tie-in: audit one SalonAppy-style component (or practice repo component) for div-soup and swap in real elements.

## ARIA basics — roles/labels for cases semantic HTML can't cover, and "no ARIA is better than bad ARIA."

<Badge type="info" text="todo" />

`02-aria-basics.js`

## Keyboard navigation & focus management — can a user tab through and operate everything without a mouse?

<Badge type="info" text="todo" />

`03-keyboard-navigation-and-focus-management.js`

Angular tie-in: Angular CDK has an a11y package (focus trap, live announcer) purpose-built for this.

## One accessibility audit — run Lighthouse or axe DevTools on any project, fix the top 3 flagged issues.

<Badge type="info" text="todo" />

`04-accessibility-audit.js`
