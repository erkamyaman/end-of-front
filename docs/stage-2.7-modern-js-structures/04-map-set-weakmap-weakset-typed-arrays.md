# Map/Set/WeakMap/WeakSet & typed arrays

<Badge type="info" text="todo" />
`stage-2.7-modern-js-structures/04-map-set-weakmap-weakset-typed-arrays.js`

when a plain object/array isn't right.

Angular tie-in: Map for keyed caches in a service (e.g. caching API responses by ID) instead of an object with string-keyed lookups.
