/**
 * async/await: sugar over Promises.
 *
 * Angular tie-in: using async/await inside an Angular service method that wraps a
 * firstValueFrom(this.http.get(...)) call, and why you can't await an Observable
 * directly.
 */
