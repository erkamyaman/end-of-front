/**
 * Callback hell & why Promises replaced it: nested pyramid-of-doom vs flat
 * .then() chains.
 *
 * Angular tie-in: seeing why RxJS operators (switchMap etc.) exist for the exact
 * same "flatten nested async" reason.
 */
