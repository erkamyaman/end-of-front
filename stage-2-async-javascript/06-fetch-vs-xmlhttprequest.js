/**
 * Fetch vs XMLHttpRequest — fetch is the modern default.
 *
 * Angular tie-in: HttpClient can be backed by either XHR or the newer fetch-based
 * backend depending on how it's configured — worth knowing which your app uses.
 */
