/**
 * Error handling in async code: try/catch with await, unhandled rejection gotchas.
 *
 * Angular tie-in: catchError in an RxJS pipe is the Observable-world equivalent of
 * try/catch. Same job, different syntax.
 */
