/**
 * Core operators: map, switchMap, mergeMap, debounceTime, takeUntil.
 *
 * Angular tie-in: you've used these blind. Now know why switchMap cancels in-flight
 * requests (e.g. search-as-you-type) while mergeMap runs them all.
 */
