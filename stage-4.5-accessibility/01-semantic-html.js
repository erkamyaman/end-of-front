/**
 * Semantic HTML — the right element instead of div-with-a-click-handler.
 *
 * Angular tie-in: audit one SalonAppy-style component (or practice repo component)
 * for div-soup and swap in real elements.
 */
