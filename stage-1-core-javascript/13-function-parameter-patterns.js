/**
 * Function parameter patterns — default params, rest params, arguments object, IIFEs.
 *
 * Angular tie-in: default values on service method params, and why @Input()
 * properties often need explicit defaults since Angular won't infer them for you.
 */
