/**
 * Control flow: if/else, switch, try/catch/finally, throw, Error objects.
 *
 * Angular tie-in: *ngIf/*ngSwitch are template-level mirrors of this, and
 * HttpErrorResponse handling in services is just try/catch with extra shape.
 */
