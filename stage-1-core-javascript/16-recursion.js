/**
 * Recursion — a function calling itself, base case + recursive case.
 *
 * Angular tie-in: rendering recursive structures like nested menus or a
 * breadcrumb trail often needs a recursive component pattern, not just a flat *ngFor.
 */
