/**
 * JSON & structured data: JSON.stringify/parse, what survives serialization.
 *
 * Angular tie-in: HttpClient auto-parses JSON for you. Know what breaks that
 * (non-JSON responses, responseType mismatches), and manual JSON handling for
 * localStorage.
 */
