/**
 * typeof operator & built-in objects: what typeof returns for each type.
 *
 * Angular tie-in: defensive typeof checks in services before touching an API
 * response shape you don't fully trust.
 */
