/**
 * Explicit binding & function borrowing: call/apply/bind.
 *
 * Angular tie-in: the classic bug of passing this.someMethod as a callback and
 * losing this. The actual mechanism behind why arrow functions or .bind(this) fix it.
 */
