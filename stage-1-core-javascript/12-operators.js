/**
 * Operators — arithmetic, comparison, logical, bitwise, assignment, unary,
 * comma, ternary, BigInt.
 *
 * Angular tie-in: Angular templates only allow a restricted operator subset
 * (no bitwise, no comma, no assignment) — knowing the full JS operator set
 * shows you exactly what template expressions are missing and why.
 */
