/**
 * The event loop, call stack, microtasks vs macrotasks — the concept that makes async click.
 *
 * Angular tie-in: why change detection sometimes runs "late" relative to when you
 * expect — Zone.js patches into this exact mechanism.
 */
