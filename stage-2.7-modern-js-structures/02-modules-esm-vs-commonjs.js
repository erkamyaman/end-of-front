/**
 * Modules (ESM vs CommonJS): import/export vs require.
 *
 * Angular tie-in: why tree-shaking works at all in an Angular production build, and
 * why standalone components/lazy-loaded routes rely on clean ESM boundaries.
 */
