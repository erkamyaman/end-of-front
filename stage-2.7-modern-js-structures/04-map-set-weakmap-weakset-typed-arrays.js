/**
 * Map/Set/WeakMap/WeakSet & typed arrays — when a plain object/array isn't right.
 *
 * Angular tie-in: Map for keyed caches in a service (e.g. caching API responses by ID)
 * instead of an object with string-keyed lookups.
 */
