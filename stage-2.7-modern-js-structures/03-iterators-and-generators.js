/**
 * Iterators & Generators — function*, yield, what makes something iterable.
 *
 * Angular tie-in: niche in everyday Angular work, but async generators are part of
 * how some newer streaming APIs behave — good to recognize even if you rarely write one.
 */
